# Travis CI Configuration

language: python
python:
  - "3.4"
install:
  - pip install coverage
  - pip install pep8
  - pip install coveralls
script:
  - coverage run -m nose
after_success:
  - coveralls
