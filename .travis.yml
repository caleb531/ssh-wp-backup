# Travis CI Configuration

language: python
python:
  - "3.4"
install:
  - pip install coverage
  - pip install pep8
  - pip install coveralls
script:
  - nosetests
  - nosetests --with-coverage --cover-erase
after_success:
  - coveralls
